default_install_hook_types:
  - pre-commit
  - commit-msg
minimum_pre_commit_version: 3.2.0

repos:
  - repo: https://github.com/tekwizely/pre-commit-golang
    rev: v1.0.0-rc.1
    hooks:
      - id: go-build-mod
      - id: go-mod-tidy
      # Unit tests only. The integration tests need a ClickHouse container
      # and are behind the integration build tag, so they stay out of the
      # commit path.
      - id: go-test-mod
      - id: go-fmt

  - repo: https://github.com/commitizen-tools/commitizen
    rev: v4.8.3
    hooks:
      - id: commitizen
      - id: commitizen-branch
        stages:
          - manual

  - repo: https://github.com/DavidAnson/markdownlint-cli2
    rev: v0.23.1
    hooks:
      - id: markdownlint-cli2

  - repo: local
    hooks:
      - id: golangci-lint
        name: golangci-lint
        description: Fast linters runner for Go
        entry: env -u GOROOT GOTOOLCHAIN=auto golangci-lint run --fix
        types: [go]
        language: system
        require_serial: true
        pass_filenames: false

      - id: golangci-lint-config-verify
        name: golangci-lint-config-verify
        description: Verifies the configuration file
        entry: env -u GOROOT GOTOOLCHAIN=auto golangci-lint config verify
        files: '\.golangci\.(?:yml|yaml|toml|json)'
        language: system
        pass_filenames: false
